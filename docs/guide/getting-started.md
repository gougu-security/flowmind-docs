# 安装与启动

本章介绍如何安装和首次启动 FlowMind。

## 下载安装

### 从 GitHub Releases 下载

1. 访问 [GitHub Releases](https://github.com/G3G4X5X6/mitm-scanner/releases) 页面
2. 根据您的操作系统下载对应的安装包：
   - **Windows**: `FlowMind-x.x.x-x64-setup.exe`
   - **macOS**: `FlowMind-x.x.x.dmg`
   - **Linux**: `FlowMind-x.x.x.AppImage` 或 `.deb`

### 安装步骤

::: code-group
```bash [Windows]
# 双击运行安装包
FlowMind-0.3.0-x64-setup.exe
```

```bash [macOS]
# 打开 DMG 文件，拖拽到 Applications
open FlowMind-0.3.0.dmg
```

```bash [Linux]
# AppImage 方式
chmod +x FlowMind-0.3.0.AppImage
./FlowMind-0.3.0.AppImage

# Debian/Ubuntu 方式
sudo dpkg -i flowmind_0.3.0_amd64.deb
```
:::

## 首次启动

### 1. 启动应用

双击应用图标启动 FlowMind。首次启动时，应用会：

- 初始化本地数据库（SQLite）
- 生成 CA 根证书（用于 HTTPS MITM）
- 加载默认配置

### 2. 界面概览

启动后，您将看到以下界面布局：

```
┌─────────────────────────────────────────────────────────┐
│  标题栏（项目切换、代理控制、浏览器启动）              │
├─────────────────────────────────────────────────────────┤
│  导航栏（转发器、拦截器、重发器、模糊器、扫描器、AI）  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    主内容区域                            │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  状态栏（代理状态、连接数、日志）                      │
└─────────────────────────────────────────────────────────┘
```

### 3. 启动代理

1. 点击标题栏的 **启动代理** 按钮
2. 默认监听 `127.0.0.1:8080`
3. 状态栏显示代理运行状态

### 4. 配置浏览器代理

将浏览器代理设置指向 FlowMind：

| 设置项 | 值 |
|--------|-----|
| HTTP 代理 | `127.0.0.1:8080` |
| HTTPS 代理 | `127.0.0.1:8080` |
| SOCKS 代理 | 不使用 |

::: tip 快捷方式
FlowMind 支持一键启动浏览器。点击标题栏的 **启动浏览器** 按钮，会自动配置代理并打开浏览器。
:::

### 5. 安装 CA 证书

为了正确解密 HTTPS 流量，需要安装 FlowMind 的 CA 证书：

1. 进入 **设置** → **证书管理**
2. 点击 **导出 CA 证书**
3. 将证书导入系统或浏览器的信任证书库

::: warning 安全提示
CA 证书仅用于本地开发和测试环境。请勿在生产环境或不受信任的网络中使用。
:::

## 下一步

- 了解 [代理核心](./proxy.md) 的详细配置
- 学习使用 [转发器](./forwarder.md) 查看流量
- 探索 [AI 功能](./ai.md) 进行安全分析
